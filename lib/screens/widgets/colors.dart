import 'package:flutter/material.dart';

// Đỏ đô (Burgundy/Maroon) - màu chính thống nhất cho toàn bộ app
const ColorbuttonColor = Color(0xFF9B3232);

// Using the hex value 0xA52A2A
const datve = Color(0xFFA52A2A);
